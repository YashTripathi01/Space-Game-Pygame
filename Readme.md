# Simple game using Python's pygame library

##### To run the script -

Clone or download the code.

```sh
cd into project directory
pip install requirements.txt
```

Run the main.py file

```sh
python main.py
```